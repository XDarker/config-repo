env: dev
spring:
  application:
    name: order

  #数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver            # mysql驱动包
    url: jdbc:mysql://localhost:3306/SpringCloud_Sell?useSSL=false&serverTimezone=GMT%2B8            # 数据库名称
    username: root
    password: root
    jpa:
      show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
